{% extends 'base.html.twig' %}

{% block title %}Edit Release - Admin{% endblock %}

{% block sidebar %}
    {{ include('project/_sidebar.html.twig') }}
{% endblock %}

{% block body %}
<div class="page-header">
    <div style="display: flex; justify-content: space-between; align-items: flex-end;">
        <div>
            <h1>Edit Release: {{ release.releasename }}</h1>
            <p class="subtitle">System Administration - Milestone Update</p>
        </div>
        <a href="{{ path('app_admin_releases') }}" class="badge badge-blue" style="text-decoration: none; margin-bottom: 0.5rem;">Back to List</a>
    </div>
</div>

<div class="project-card" style="max-width: 800px;">
    <form method="post">
        <div class="form-group">
            <label for="project_id">Project</label>
            <select id="project_id" name="project_id" class="form-control" required>
                {% for project in projects %}
                    <option value="{{ project.project_id }}" {% if project.project_id == release.project_id %}selected{% endif %}>{{ project.project_name }}</option>
                {% endfor %}
            </select>
        </div>

        <div class="form-group">
            <label for="name">Release Name</label>
            <input type="text" id="name" name="name" class="form-control" value="{{ release.releasename }}" required>
        </div>

        <div class="form-group">
            <label for="description">Description</label>
            <textarea id="description" name="description" class="form-control" style="min-height: 120px;">{{ release.description }}</textarea>
        </div>

        <div style="margin-top: 2rem; display: flex; gap: 1rem;">
            <button type="submit" class="btn-primary">Save Changes</button>
            <a href="{{ path('app_admin_releases') }}" class="btn-primary" style="background: rgba(255,255,255,0.05); color: var(--text-muted); text-decoration: none;">Cancel</a>
        </div>
    </form>
</div>
{% endblock %}
