@page "/testsuites/details/{Id:int}"
@rendermode InteractiveServer
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Identity
@using Microsoft.EntityFrameworkCore
@using LoreTest.Data
@inject ApplicationDbContext DbContext
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject UserManager<ApplicationUser> UserManager
@inject NavigationManager NavigationManager
@attribute [Authorize]

<PageTitle>Test Suite Details</PageTitle>

@if (testSuite == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <div class="d-flex justify-content-between align-items-center">
        <h1>Test Suite: @testSuite.Title</h1>
        @if (canEdit)
        {
            <div>
                <a href="/testsuites/edit/@testSuite.Id" class="btn btn-outline-primary btn-sm">Edit Suite</a>
                <a href="/testsuites/delete/@testSuite.Id" class="btn btn-outline-danger btn-sm ms-1">Delete Suite</a>
            </div>
        }
    </div>

    @if (!string.IsNullOrWhiteSpace(testSuite.Description))
    {
        <div class="mb-3 mt-2">@((MarkupString)(testSuite.Description))</div>
    }

    <hr />

    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Test Cases</h2>
        @if (canEdit)
        {
            <a href="/testcases/create?suiteId=@testSuite.Id" class="btn btn-primary btn-sm">+ Add Test Case</a>
        }
    </div>

    @if (!testSuite.TestCases.Any())
    {
        <p>No test cases found in this suite.</p>
    }
    else
    {
        <table class="table table-bordered table-hover">
            <thead class="table-dark">
                <tr>
                    <th style="width: 6%">ID</th>
                    <th>Title</th>
                    <th style="width: 10%">Priority</th>
                    <th style="width: 10%">Status</th>
                    <th style="width: 18%">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var tc in testSuite.TestCases.OrderBy(t => t.Id))
                {
                    <tr>
                        <td>@tc.Id</td>
                        <td>@tc.Title</td>
                        <td>
                            <span class="badge @(tc.Priority == "High" ? "bg-danger" : tc.Priority == "Medium" ? "bg-warning text-dark" : "bg-secondary")">
                                @tc.Priority
                            </span>
                        </td>
                        <td>
                            <span class="badge @(tc.Status == "Pass" ? "bg-success" : tc.Status == "Fail" ? "bg-danger" : "bg-warning text-dark")">
                                @tc.Status
                            </span>
                        </td>
                        <td>
                            <a href="/testcases/details/@tc.Id">View</a>
                            @if (canEdit)
                            {
                                <span> | </span>
                                <a href="/testcases/edit/@tc.Id">Edit</a>
                                <span> | </span>
                                <a href="/testcases/delete/@tc.Id">Delete</a>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }

    <div class="mt-4">
        <a href="/projects/details/@testSuite.TestProjectId" class="btn btn-secondary">Back to Project</a>
    </div>
}

@code {
    [Parameter]
    public int Id { get; set; }

    private TestSuite? testSuite;
    private bool canEdit = false;

    protected override async Task OnInitializedAsync()
    {
        var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        var user = await UserManager.GetUserAsync(authState.User);

        if (user != null && (user.Role == "Administrator" || user.Role == "Editor"))
        {
            canEdit = true;
        }

        testSuite = await DbContext.TestSuites
            .Include(s => s.TestCases)
            .FirstOrDefaultAsync(m => m.Id == Id);

        if (testSuite == null)
        {
            NavigationManager.NavigateTo("/projects");
        }
    }
}
