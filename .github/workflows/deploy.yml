name: CI/CD Pipeline

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build --no-restore
      - name: Test
        run: dotnet test LoreTest.Tests/LoreTest.Tests.csproj --no-build --verbosity normal

  build-and-push:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=sha

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  deploy-test:
    needs: build-and-push
    if: github.ref == 'refs/heads/develop'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Deploy to Test Environment
        run: |
          echo "Deploying to test environment (Localhost)..."
          docker compose -f docker-compose.test.yml pull
          docker compose -f docker-compose.test.yml up -d

  deploy-preprod:
    needs: build-and-push
    if: github.ref == 'refs/heads/main'
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Deploy to PreProd Environment
        run: |
          echo "Deploying to preprod environment (Localhost)..."
          docker compose -f docker-compose.preprod.yml pull
          docker compose -f docker-compose.preprod.yml up -d

  playwright-tests:
    needs: [deploy-preprod, deploy-test]
    if: always() && (needs.deploy-preprod.result == 'success' || needs.deploy-test.result == 'success')
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Build Playwright Tests
        run: dotnet build LoreTest.Playwright/LoreTest.Playwright.csproj
      - name: Run Playwright Tests
        env:
          BASE_URL: ${{ github.ref == 'refs/heads/main' && (secrets.PREPROD_URL || 'http://localhost:5002') || (secrets.TEST_URL || 'http://localhost:5001') }}
          PLAYWRIGHT_BROWSERS_PATH: C:\actions-runner\_work\ms-playwright
        run: dotnet test LoreTest.Playwright/LoreTest.Playwright.csproj --no-build --verbosity normal --logger trx --results-directory LoreTest.Playwright/bin/Debug/net10.0/allure-results
      - name: Upload Allure Results Artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: allure-results
          path: LoreTest.Playwright/bin/Debug/net10.0/allure-results/
          retention-days: 1

  publish-report:
    needs: playwright-tests
    if: always() && (needs.playwright-tests.result == 'success' || needs.playwright-tests.result == 'failure')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download Allure Results
        uses: actions/download-artifact@v4
        with:
          name: allure-results
          path: allure-results
      - name: Fix Cross-Platform Path Separators in TRX
        run: |
          cat << 'EOF' > fix-trx.py
          import os, re
          results_dir = 'allure-results'
          if os.path.exists(results_dir):
              for f in os.listdir(results_dir):
                  if f.endswith('.trx'):
                      p = os.path.join(results_dir, f)
                      print(f"Fixing paths in: {p}")
                      with open(p, 'r', encoding='utf-8') as file:
                          c = file.read()
                      c = re.sub(r'path="([^"]+)"', lambda m: 'path="' + m.group(1).replace('\\', '/') + '"', c)
                      with open(p, 'w', encoding='utf-8') as file:
                          file.write(c)
          EOF
          python fix-trx.py
      - name: Load Allure Report History
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages
      - name: Build Allure Report
        uses: simple-elf/allure-report-action@master
        id: allure-report
        with:
          allure_results: allure-results
          gh_pages: gh-pages
          allure_report: allure-report
          allure_history: allure-history
      - name: Deploy Allure Report to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-history

